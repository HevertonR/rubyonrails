<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "Informe seu novo post" %>
  </div>
  <%= f.submit "Confirmar", class: "btn btn-primary" %>
<% end %>
